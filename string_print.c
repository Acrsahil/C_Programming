#include<stdio.h>

int main(){
    // char ptr[] = "sahil is a good boy"; we can also do this by initializing array
    char *ptr = "sahil is a good boy";
    printf("Your String is : %s ",ptr);
    return 0;
}